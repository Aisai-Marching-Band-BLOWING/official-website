---
const { size, color } = Astro.props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  width={size}
  fill={color}
  class="external-icon"
  viewBox="0 0 16 16"
>
  <path
    fill-rule="evenodd"
    d="M14 2.5a.5.5 0 0 0-.5-.5h-6a.5.5 0 0 0 0 1h4.793L2.146 13.146a.5.5 0 0 0 .708.708L13 3.707V8.5a.5.5 0 0 0 1 0z"
  ></path>
</svg>

<style>
  .external-icon {
    opacity: 0.8;
    transition: all 0.3s ease;
    border-radius: 50%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    padding: 4px;
  }

  /* ホバー効果は親要素の .contact-link:hover で適用される */
  :global(.contact-link:hover) .external-icon {
    opacity: 1;
    transform: translate(2px, -2px);
    background: rgba(var(--link-rgb), 0.15);
    fill: rgb(var(--link-rgb));
  }

  /* より汎用的なホバー効果（親がリンクの場合） */
  :global(a:hover) .external-icon {
    opacity: 1;
    transform: translate(2px, -2px);
    background: rgba(var(--link-rgb), 0.15);
    fill: rgb(var(--link-rgb));
  }

  /* モバイルサイズでのアイコン中央配置 */
  @media (width < 640px) {
    .external-icon {
      margin: 0 auto;
      align-self: center;
    }
  }
</style>
